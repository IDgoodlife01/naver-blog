<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
</head>
<body>

<h1>Network Blog HOME</h1>

<% if (user) { %>
  <p><%= user.username %>님 환영합니다</p>
  <a href="/post/list">글 목록</a><br>
  <a href="/post/write">글쓰기</a><br>
  <a href="/logout">로그아웃</a>
<% } else { %>
  <a href="/login">로그인</a><br>
  <a href="/register">회원가입</a>
<% } %>

<% if (user && user.username === 'admin') { %>
  <a href="/admin/users" style="color: red;">[회원 관리 페이지]</a>
<% } %>

</body>
</html>
